version: '2'

services:
  container:
    image: "studio-manager:latest"
    links:
      - "database:database"
    environment:
      NPS_ENV: "cloud"
    networks:
      - proxy
    volumes:
      - "app:/app"
    labels:
      - "traefik.enable=true"
      - "traefik.frontend.rule=Host:studio-manager.nodea.studio"
      - "traefik.port=1337"
  database:
    image: "nodea-database"
    environment:
      MYSQL_DATABASE: "studio-manager"
      MYSQL_USER: ""
      MYSQL_PASSWORD: ""
      MYSQL_ROOT_PASSWORD: ""
    networks:
      - proxy
    volumes:
      - "db_data:/var/lib/mysql"
      - "db_log:/var/log/mysql"

volumes:
  app:
  db_data:
  db_log:
 
networks:
  proxy:
    external:
      name: proxy
